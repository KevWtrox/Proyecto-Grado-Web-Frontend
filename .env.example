# URL del API Gateway (NGINX en puerto 8080).
# En producción con Cloudflare Tunnel, reemplazar por la URL pública del túnel.
# Ejemplo: VITE_GATEWAY_URL=https://tu-subdominio.trycloudflare.com
VITE_GATEWAY_URL=http://localhost:8080
